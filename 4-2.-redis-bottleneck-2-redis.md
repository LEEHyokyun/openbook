---
description: Redis 부가 속성에 대하여
---

# 4-2. Redis 활용 및 병목 현상 (Bottleneck) #2 : Redis 부가 속성

### RESP (REdis Serialization Protocol)

RESP는 Redis에서 사용하는 핵심 통신 프로토콜로, Redis 클라이언트와 Redis 서버 간의 요청과 응답을 표현하는 직렬화 프로토콜이다.

* **RESP 프로토콜의 채택 배경 및 필요성**
  * TCP 위에서 문자열, 숫자뿐만 아니라 List, Set 등 다양한 구조적 자료구조에 대한 직렬화 및 역직렬화 처리가 필수적으로 요구되었다.
  * Redis가 지향하는 설계 사상에 부합하도록 구조가 단순하고, 실행 속도가 빠르며, 구문 분석(Parsing)이 극도로 손쉬운 전용 프로토콜 체계가 필요하였다.
* **프로토콜의 구조적 특징**
  * 데이터 타입을 명확하게 구분하고 파싱을 간단하게 처리하기 위해 철저한 '타입 기반 프로토콜' 체계를 채택하였다.
  * 대표적으로 Simple String(Char)을 나타내는 `+`, Integer를 나타내는 `:`, Array를 표현하는 `*`, Bulk String(String)을 지정하는 `$` 등의 접두 부호를 활용하여 데이터 타입을 명확히 식별하고 구조를 단순화한다.

### Redis를 채택하기 위한 타 저장소와의 비교 고려요소

**RDBMS와의 비교 (vs RDBMS)**

* **응답 Latency의 혁신적인 차이**: 전통적인 RDBMS가 약 500ms \~ 1sec 수준의 응답 지연 시간(RTT)을 보이는 반면, Redis는 100ms 수준의 극도로 낮은 응답 Latency를 보장한다(단, Redis의 설계 사상에 적합한 트랜잭션 및 통신이 이루어졌다고 가정하였을경우).
* **인메모리 다운 설계 사상**: Key-Value 기반의 단순한 구조를 넘어 List, Set, String, SortedSet 등 단순하지만 강력하고 다양한 자료구조를 기본 지원한다. 이에 더해 단순 명료하게 동작하는 Lua Script 및 전용 통신 프로토콜(RESP)을 결합하여 인메모리 저장소로서의 활용성을 극대화한다.

**MongoDB와의 비교 (vs MongoDB)**

* **실시간 Hot Data 처리 및 무결성 우위**: MongoDB는 기본적으로 디스크 기반의 저장 체계를 사용하므로 쿼리 구조가 매우 복잡하며, 실시간으로 발생하는 핫 데이터의 초고속 연산 처리와 멀티 도큐먼트 간의 트랜잭션 원자성(Atomicity) 보장이 불가물하다.
* **데이터 모델링 특성**: MongoDB는 EAV(Entity-Attribute-Value) 모델이나 JSON 기반의 동적 속성 추가 및 중첩 구조에 대응하기 위해 스키마 제약이 없는 데이터 모델링을 제공한다는 특징이 있으나, 실시간 고속 연산 측면에서는 Redis가 확실한 판정승을 거둔다.

**Memcached와의 비교 (vs Memcached)**

* **다양한 연산 및 자료구조의 확장성**: Memcached는 Redis와 동일하게 RAM 영역에 데이터를 저장한다는 하드웨어적 공통점을 가진다. 그러나 Redis에 비해 자료구조가 오직 String-String 형태로만 제한되어 있어 효율적인 캐시 처리와 다양한 연산 처리를 수행하기 어렵다.
* **데이터 보존 및 실행 모델의 한계**: Memcached는 만료 시간(TTL) 설정이 없거나 미비하여 시스템을 재시작하는 즉시 메모리 내부의 데이터가 그대로 폐기된다. 또한 Blocking 기반의 Multi Thread 방식으로 동작하기 때문에, 본질적으로 인메모리 자원의 극대화에는 적합하지 않은 비효율적인 아키텍처 설계를 취하고 있다.

**Redis Stream과의 비교 (vs Redis Stream)**

* **이벤트 수준의 메시지 영속화**: RabbitMQ나 Apache Kafka처럼 시스템 간에 유입되는 "이벤트" 수준의 메시지를 전혀 유실하지 않고 안전하게 저장하고 관리하기 위해 Redis Stream 아키텍처를 도입하여 활용할 수 있다.
* **스레드 및 메시지 대기 메커니즘**: Consumer 측에서 데이터를 수신하기 위해 주기적으로 Polling을 수행하거나, 일정 시간 동안 Blocking 방식을 채택하여 스레드를 점유한 상태로 메시지를 대기하는 제어 구동 체계를 구축한다.

### Redis의 영속성 (Persistence) 아키텍처와 복구 전략

**영속성 설정의 근본적인 필요성** Redis는 기본적으로 휘발성 자원인 RAM에 데이터를 저장하는 구조이다. 이로 인해 운영 중 Redis 설정 정보의 변경 사항을 시스템에 반영하거나 기타 사유로 Redis 서버를 재실행(Reboot)하게 되면 메모리 내부의 데이터는 그대로 폐기된다. 따라서 장애 발생 시 데이터를 추적하고, 시스템의 고가용성(HA) 및 철저한 이력 관리를 달성하기 위해서는 반드시 디스크 장치를 매개로 한 영속성 설정을 구동하여 시스템 다운 시 디스크에서 데이터를 완벽히 복구할 수 있어야 한다.

**RDB Snapshot 메커니즘**

* RAM 메모리는 물리적으로 페이지(Page) 단위로 데이터를 관리하며 저장한다.
* 백그라운드 백업을 위해 자식 프로세스(Child Process)가 생성되어 기존 메모리 상태를 읽어 나가는 도중, 메인 스레드에서 새로운 연산 및 쓰기 명령이 발생하면 OS 레벨의 Copy On Write (COW) 메커니즘이 발동된다.
* Copy On Write를 통해 변경이 발생한 해당 메모리 페이지를 안전하게 복사한 후 실제 연산을 수행하므로, 메인 스레드의 읽기 및 디스크 덤프(Dump) 프로세스는 상호 간섭이나 성능 저하 없이 소기 성능을 그대로 유지하며 안전하게 수행된다.

**AOF (Append Only File) 메커니즘**

* 시스템 내부의 대대적인 연산 명령어를 디스크에 위치한 `appendonly.aof` 파일에 지속적으로 기록하는 방식이며, 관련 상세 제어 옵션은 `redis.conf` 환경 설정 파일 내에 존재한다.
* 데이터를 저장할 때 별도의 가공을 거치지 않고, 네트워크로 유입된 RESP 형태의 명령어를 문자열 텍스트 파일 구조 그대로 디스크에 덤프한다. AOF 버퍼(Buffer) 공간에 차곡차곡 쌓인 명령어들을 순차적으로 파일 끝에 추가(Append)하는 방식으로 무결성을 확보한다.

**Hybrid 복구 아키텍처**

* 주기적으로 AOF 로그 파일 기록을 진행하되, 파일 용량 비대화를 막기 위해 대대적인 최적화를 수행하는 리라이트(Rewrite) 시점을 기준으로 삼는 전술이다.
* Rewrite 시점을 기준으로 파일의 앞부분은 바이너리 형태의 RDB Snapshot 데이터로 압축하여 채워 넣고, 리라이트 이후에 발생하는 뒷부분의 변경 내역들은 AOF의 순수 텍스트(Text) 형태로 연이어 결합하는 이중 구조를 취한다.
* 결과적으로 현재 상태의 Snapshot (Binary) 구조와 이후의 변경 상태 (Text)가 유기적으로 공존하는 하이브리드 파일 레이아웃이 완성되며, 이를 통해 디스크를 통한 데이터 복구 성능과 재구동 속도를 획기적으로 향상시킨다.
