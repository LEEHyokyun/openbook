---
description: Java를 깊이 있게 활용하기 위한 방법, 직렬화에 대하여
---

# 1-7. Java 언어의 깊이: 직렬화

### 1. Serializable 인터페이스의 역할과 분산 환경에서의 필요성

* JPA 엔티티에서 Serializable 인터페이스를 구현한 것과 구현하지 않은 것은 일반적인 상황에서 차이가 거의 없다.
* 생성, 조회, 수정, 삭제와 같은 기본적인 CRUD 트랜잭션 역시 정상적으로 동작한다.
* 분산 환경에서의 예외 발생: 애플리케이션 환경이 WAS 클러스터와 같은 분산 환경으로 바뀌거나, 특히 엔티티를 메시지 큐 기반의 외부 체계(RabbitMQ, Redis, Kafka 등)에 페이로드로 담아 전송해야 할 경우 구현하지 않았다면 예외가 발생한다.
* 프록시 객체 기반의 제어: 환경이 변경되었을 때 특히 프록시 객체 기반의 직렬화 및 역직렬화가 필요하다면, Serializable을 구현한 엔티티에 대해서만 안전하게 연산 처리가 가능하다.
* 직렬화 활용의 핵심 경계: JPA의 핵심은 직렬화가 아니다. 데이터베이스와 JVM 메모리 사이에서의 영속성 컨텍스트 관리가 본질적 핵심이다.
  * 1차 캐시: 영속성 컨텍스트 내부에서 관리되므로 직렬화가 수행되지 않는다.
  * 2차 캐시: JVM 간의 공유, 디스크 저장, 혹은 외부 캐시 체계와 연동되므로 직렬화 명세가 필수적으로 요구된다.
* 결론적으로 외부 체계로의 데이터 직렬화가 필요할 때, 혹은 HTTP 세션에 세션 복제 및 엔티티 저장이 필요할 때 이 인터페이스의 구현이 필연적으로 수반된다.

### 2. 직렬화(Serialization)와 역직렬화(Deserialization)의 정의 및 연산 기준

* 직렬화와 역직렬화 연산의 절대적인 판정 기준은 JVM 메모리다.
* 직렬화: JVM 메모리 공간 안에 존재하는 객체 구조를 외부로 전달 가능한 형태로 변환하는 메커니즘을 의미한다.
  * 객체(Object)를 파일(CSV, JSON) 및 보통은 바이너리 코드인 바이트 배열(byte\[])로 변환하는 연산이다.
  * 네트워크 통신(Kafka, HTTP) 및 외부 체계(Redis, 데이터베이스)와의 통신 과정에 쓰이는 변환 제어가 모두 이에 포함된다.
* 역직렬화: 외부 시스템의 데이터를 JVM 메모리 안에서 실행 가능한 객체 형태로 변환하는 메커니즘을 의미한다.
* 방향성 매커니즘:
  * JVM 메모리에서 외부 데이터 방향으로 나아가면 직렬화다.
  * 외부 데이터에서 JVM 메모리 방향으로 들어오면 역직렬화다.

### 3. 객체 변환 API 및 직렬화 과정에서의 유의사항

* 직렬화 및 역직렬화 연산 시 자바 내부적으로는 ObjectOutputStream, ObjectInputStream 등의 표준 API 스트림 클래스를 활용하여 데이터를 물리적으로 변환하고 전달한다.
* 변환 과정의 본질적 유의사항: Object에서 String으로 변환되거나, String에서 최종적인 byte\[] 바이트 배열로 변환되는 각각의 개별적인 변환 과정들 역시 모두 직렬화와 역직렬화 체계의 파편이자 일부라는 사실을 명확히 인지하고 설계해야 한다.
