---
description: 단순 가상화를 넘어 프로젝트 체계를 분리하고 운영하기 위한 체제의 관점으로 바라본 Docker
---

# 6-2. Docker #3 : Swarm / Service / Stack

### 1. Docker Swarm

Docker Swarm / Service는 표면적으로 단순히 멀티 호스트의 멀티 컨테이너를 묶는 단위이다. 하지만, 내면적으로는 하나의 애플리케이션(프로젝트)를 구성하는, 실행하는데 필요한 논리적 요소라는 매우 중요한 의미를 지닌다.&#x20;

* 즉, Docker Swarm은 이러한 멀티 호스트의 분산된 컨테이너들을 하나의 클러스터로 묶고, 사람이 관리할 수 있도록 간편한 기능과 체계를 제공하는 기술이다. 바로 이러한 기술이 쿠버네티스의 본질과 많이 닮았는데, 사람이 관리하기 힘든 부분에 대해 간편한 기능을 제공하는 것이 표면적 활용 가치이지만, 서비스를 운영하고 체계를 이해하는데 필요한 모델링의 개념으로 활용할 수 있는 것이 Docker Swarm의 진정한 의미이다.
* 이 Swarm의 체계에서 논리적 단위, 체계, 상태로 제공하는 것이 Service 및 Stack이다

```
Swarm

├── Node
├── Service
├── Task
└── Network
```

### 2. Docker Service / Stack

Docker Service는 Swarm에 등록되어있는 노드들을 관리하고 목록을 확인하는 용도가 아닌, Swarm 클러스터 전체에서 각 노드들의 실행 상태를 정의하는 객체이다.&#x20;

즉, 이에 대한 상태를 Manager 노드 측에 기억해두도록 하여, 번거로운 상태 유지 작업없이 그 "상태"를 기억하도록 하고 이를 "객체화"하여 원하는 Swarm의 상태를 유지하고 간편하게 실행할 수 있도록 도와주는 스위치같은 역할을 하는 것이다.

Docker Stack은 하나의 프로젝트, 서비스(다만 본 내용에서는 Docker Service와 구분하기 위해 프로젝트라는 단어를 사용)를 위해 여러 멀티 컨테이너 클러스터(Docker Service)를 논리적으로 묶어, 프로젝트 실행에 필요한 서비스들을 그룹화하는 것이다.

```null
Docker Swarm Cluster
│
├── Stack (프로젝트 단위)
│     │
│     ├── Service (논리적 서비스)
│     │      │
│     │      ├── Task
│     │      │      │
│     │      │      └── Container
│     │      │
│     │      └── Task
│     │             │
│     │             └── Container
│     │
│     └── Service
│             │
│             └── Task
│                    │
│                    └── Container
│
└── Node
      ├── Manager
      └── Worker
```



### 3. 모델링 관점에서의 Docker 3S

물론 멀티 호스트에 나뉘어진 멀티 컨테이너들을 손쉽게 관리하고 운영 비용을 낮추는데 필요한 핵심 기술은 맞지만, Docker 3S는 이를 넘어 데이터 모델링, 프로젝트 구성 관점에서 고민점을 제시해주는 이정표이기도 하다. 즉, 단순히 "묶는다"는 개념이 아닌, 데이터 모델링의 관점에서 명확한 목적과 역할을 가지고 도메인을 분리하고 이를 체계화하는 과정(혹은Role)이기도 한 것이다.

이는 단순히 멀티 레포, 모노 레포 등 배포에 대한 개념이 아니다.

하나의 큰 프로젝트를 Stack으로 모델링하여 각 구성요소를 정의하거나,

```
Shopping Mall(Stack)
├── frontend(Service)
├── backend(Service)
├── redis(Service)
└── mysql(Service)

```

단순하게 알고 있던 DB Per Service의 분리 단위, 운영 체계(물리적/논리적 소유권 및 도메인 등), 책임 범위에 대해 고민하고 이에 따른 데이터 모델링을 어떻게 구현할지 그 본질적인 고민을 하게 해주는 기술이기도 하다.

```
Shopping Platform(Stack)
├── gateway(Service)
│      ├── gateway.1
│      └── gateway.2
│
├── member-service(Service)
│      ├── member.1
│      ├── member.2
│      └── member.3
│
├── order-service(Service)
│      ├── order.1
│      └── order.2
│
├── product-service(Service)
│      └── product.1
│
├── payment-service(Service)
│      └── payment.1
│
├── notification(Service)
│      └── notification.1
│
├── kafka(Service)
│      ├── kafka.1
│      ├── kafka.2
│      └── kafka.3
│
├── redis(Service)
│      └── redis.1
│
├── member-db(Service)
│      └── mysql.1
│
├── order-db(Service)
│      └── mysql.1
│
└── product-db(Service)
       └── mysql.1

```

Service는 위와 같이, 도메인 분리 및 기술 분리(Kafka/Redis 등)과 이에 얽힌 책임/물리적 혹은 논리적 소유권에 대한 도식을 제공한다.

```
대규모 MSA
──────────────────────────────

Member Stack
├── member-service
└── member-db (MySQL Cluster)

Order Stack
├── order-service
└── order-db (MySQL Cluster)

Product Stack
├── product-service
└── product-db (MySQL Cluster)
```

규모가 커진다면, MSA 전체가 아닌 각 도메인을 하나의 Stack으로 분리하여 이에 따른 모델링 및 책임 격리 등 명확한 목적 및 역할을 정의하기 위한 본질적인 의문을 제시하는 것이다.
