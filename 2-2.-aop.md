---
description: 공통 관심사, AOP에 대하여
---

# 2-2. AOP

### 1. AOP(Aspect Oriented Programming)의 정의와 공통 관심사 분리 메커니즘

* AOP는 관점 지향 프로그래밍을 의미한다.
* 비즈니스 로직의 공통 관심사를 지정하여 이를 별도의 도메인 및 계층으로 분리한다.
* 분리된 공통 로직을 하나의 관점(Aspect)으로 빼서 구성한 뒤, 이를 자동으로 필요한 시점에 끼워 넣는 프로그래밍 방법이다.

### 2. Advice의 정의와 실행 시점 제어

* Advice는 무엇을, 어느 시점에 실행할지에 대한 개념이다.
* 무엇을 관점으로 볼 것인가, 무엇을 공통적인 관심사로 지정할 것인가로 "어떤 행위를 공통적으로 실행할지"에 대한 항목을 명세한다.
* 이 Advice를 "어느 시점"에 적용할 것인지에 대한 정의도 포함하는 개념이다.
* 구체적인 실행 시점 제어 조건으로는 After, Before, Around 등이 존재한다.
* 자바 애노테이션 기반 구체적 예시 구조 : 명세와 같이 특정 연산(어드바이스)이 정상적으로 반환 및 완료된 시점(포인트컷, @AfterReturning)에 핸들러 메서드 로직을 실행하도록 시점을 제어한다.
  * ```
    @AfterReturning(pointcut = "@annotation(CachePut)")
    public void handleCachePut(JoinPoint joinPoint, CachePut cachePut)) 
    ```

### 3. Pointcut의 정의와 Join Point 필터링 메커니즘

* Pointcut은 어느 위치에, 어느 대상으로 AOP를 적용할 것인지에 대한 대상 및 범위를 의미한다.
* Advice를 "어디에" 적용할 것인지 제어하며, 시간적 시점이 아닌 메서드, 클래스, 패키지 단위로 "위치와 대상"을 지정하는 개념이다.
* Join Point는 메서드 실행 및 생성자 호출 등 AOP를 적용할 수 있는 프로그램 내의 모든 실행 지점을 뜻한다.
* Pointcut은 이 적용 가능한 모든 지점인 Joint Point 중 정확히 어디에 적용할 것인지 필터링한 개념에 해당한다.
* 애플리케이션 환경에서 보통은 메서드 실행 지점을 타깃으로 잡고 필터링을 수행한다.

### 4. Aspect와 AOP의 핵심 차이점

* Aspect는 개별적으로 분리된 핵심 개념들을 하나로 조합한 결과물이다.
* 무엇을 실행할지 정의한 Advice와 어디에 적용할지 필터링한 Pointcut 등의 개념들을 유기적으로 조합하여, 공통 관심사를 하나의 모듈 혹은 체계로 구성해 놓은 구성을 Aspect라 한다.
* AOP는 이러한 Aspect들을 활용하여 공통 관심사를 격리하고 필요한 시점에 자동으로 끼워 넣는 패러다임이자 프로그래밍 방법론 전체를 아우르는 상위 개념이다.
