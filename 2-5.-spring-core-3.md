---
description: Spring framework의 core - 부가 설정에 대하여
---

# 2-5. Spring Core #3 : 부가 설정

### 1. JPA Hibernate Dialect의 정의와 DB 질의문 작성을 위한 구조적 역할

* Dialect의 핵심 개념: Hibernate 프레임워크 측에서 자바 비즈니스 요청에 따른 ORM SQL 질의를 각 데이터베이스 제품군(MySQL, Oracle, PostgreSQL, H2 등)의 고유한 문법 체계에 맞게 동적으로 변경 및 조율하기 위해 개입하는 교량 역할을 수행한다.
* 선택 중심의 메커니즘 흐름: 개발자가 엔티티(Entity), JPQL, Criteria, Repository 메서드 형태로 비즈니스 코드를 작성하면, Hibernate ORM 엔진은 이를 해석한 뒤 Dialect에 설정된 해당 DB별 SQL 생성 규칙을 참조한다. Dialect는 단순한 문법 "변경"이라기보다는 사전에 정의된 규칙 리스트 중 타깃 DB에 일치하는 쿼리문을 "선택"하여 JDBC Driver로 인계하고, 최종적으로 Database에 격리된 질의가 도달하도록 가이드한다.
* 아키텍처적 구동 계층 흐름: \[작성한 코드 (Entity, JPQL, Repository)] -> \[Hibernate ORM 엔진] -> \[Dialect (DB별 SQL 생성 규칙)] -> \[JDBC Driver] -> \[Database]

### 2. spring-boot-starter-web 의존성을 통한 내장 Tomcat 및 Web Server 구동 프로세스

* 클래스패스(classpath) 활성화와 자동 설정 감지 단계: 프로젝트 메이븐이나 그레이들 설정에 spring-boot-starter-web 의존성이 주입되면, 애플리케이션 구동 시 클래스패스 상에 서블릿(Servlet) 및 내장 웹 서버 관련 핵심 클래스들이 일제히 배치된다. 이를 확인한 Spring Boot의 AutoConfiguration(자동 설정) 메커니즘이 동작하여 내부적으로 톰캣(Tomcat) 등 서블릿 기반의 내장 웹 서버를 빌드하고 띄울 기초적인 기초 준비를 수행한다.
* ApplicationContext 실행 및 refresh 시점의 실질 구동 단계: 단순 라이브러리 참조 단계를 넘어, 스프링 부트 애플리케이션이 구동되면서 메인 컨텍스트의 refresh() 라이프사이클 시점에 다다르면 실제 내장 톰캣 인스턴스가 물리적으로 생성된다. 이 시점에 지정된 네트워크 포트 바인딩(Port Binding)이 집행되고 서블릿 컨테이너 초기화 연산이 완수된다.
* 인프라 준비 완료 및 REST API 활성화: 위와 같은 Web Server 사용 준비 시퀀스가 내부 프레임워크 경계 내에서 완벽하게 완료됨에 따라, 개발자는 별도의 외곽 WAS 설치 과정 없이도 즉각적으로 REST API 프로토콜 엔드포인트를 개방하고 클라이언트 통신을 접수할 수 있는 상태에 도달한다.

### 3. ddl-auto 속성과 Schema.sql 기반 데이터베이스 초기화 기법의 특성 비교

* ddl-auto의 동작 방식과 환경 독립성: JPA Hibernate 설정에서 제공하는 ddl-auto(update, create, create-drop 등) 속성은 어플리케이션 구동 시점에 자바 엔티티(Entity) 클래스들의 구조를 직접 분석하여 데이터베이스 레이어의 테이블을 자동으로 제어 및 생성해 준다. 이는 코드를 기반으로 테이블을 유추하므로 환경 독립성이 매우 강력하다는 이점이 있으나, 실제 운영 인프라 환경이나 고도로 복잡한 DB 구조 환경에서는 미세하고 정밀한 제어가 어렵다는 한계를 지닌다.
* schema.sql의 환경 종속성과 정밀 제어: 이와 반대로 Spring Boot 자체에서 항상 가동할 수 있는 sql init mode를 활성화하여 schema.sql 파일을 직접 구동하는 방식은, 개발자가 특정 벤더 DB용 ddl 문법을 직접 기술하므로 세밀한 파일 구조 제어가 가능하다. 다만, 데이터베이스 제품군을 변경하는 경우 해당 쿼리문 문법을 수동으로 재작성해야 하므로 환경 종속적인 성격을 명확히 띤다. 두 초기화 방식 간의 원천적인 성능 차이는 미비한 수준이다.
