---
description: CS 기초 및 알고리즘에 대하여 - 2장
---

# 1-1. CS / Basic #2

***

### 1. 메모리 용량 단위 정리

* 기본 1 bit
* 1 Byte = 8 Bit
* 1 KByte = 1024 Byte
* 1 MByte = 1024 KByte
* 1 GByte = 1024 MByte
* 1 TByte = 1024 GByte

### 2. StringBuilder, StringBuffer, String 차이점

* StringBuilder는 가변(mutual) 객체이다.
  * StringBuilder 객체를 중복으로 생성하지 않는다. 하지만, 내부적으로 char 배열의 내용을 직접 바꿀 수 있다.
* StringBuffer는 가변(mutual) 객체이다.
  * StringBuilder와 동일하다. 하지만 내부 배열의 내용을 수정할 때 동기화 처리(synchronized)가 추가된다.
* String은 불가변(Immutable) 객체이다.
  * String 객체는 한번 생성한 후 그 값을 절대 수정할 수 없다.
  * replace 등의 API 결과를 추출하기 위해서는 반드시 다른 변수를 사용해야 하며, API 사용 이후에도 객체는 불변이다.
  * 직접 할당의 경우 String pool에, new String 객체는 heap에 변수의 value가 위치한다.
  * ```
    String a = "hello"; 
    String b = "hello"; 
    System.out.println(a == b); // true (String pool 참조 일치) 
    String c = new String("hello"); System.out.println(a == c); // false (heap 객체 주소 다름)
    ```

### 3. == 연산자와 equals 메서드 차이

* \== 연산자: 참조하는 주소값의 일치 여부, 즉 동일한 객체인지를 확인한다. int와 같은 primitive 타입의 경우 객체 참조가 아닌, 값 자체가 stack에 들어있고 이 값을 비교하기에 주소값 비교가 아닌 내부 값 비교가 성립된다.
* equals 메서드: 논리적으로 동일한 값을 가지고 있는 객체인지를 확인한다. (동등성 비교)

### 4. 오토 박싱이란 무엇인가요?

* 자바 컴파일러가 원시 타입(Primitive type)을 그에 상응하는 래퍼 클래스(Wrapper class) 객체로 자동 변환해 주는 것.
* 반대로 래퍼 클래스를 원시 타입으로 바꾸는 것은 언박싱(Unboxing)이라 부른다.

### 5. 얕은 복사와 깊은 복사의 차이점

* 얕은 복사(Shallow Copy): 객체의 주소값만을 복사하는 형태. 복사된 객체가 원본 객체와 동일한 메모리 주소를 가리키므로, 복사본을 수정하면 원본 데이터에도 영향을 준다.
* 깊은 복사(Deep Copy): 실제 데이터 자체를 완전히 새로운 메모리 공간에 복사하는 형태. 원본과 복사본이 완전히 격리되므로 한쪽을 수정해도 서로 영향을 주지 않는다.

### 6. static 블록의 용도와 실행 시점

* 정적(static) 멤버 변수를 초기화하기 위해 사용하는 특수 블록이다.
* 실행 시점: JVM에 의해 클래스가 메모리에 최초로 로딩되는 시점에 단 한 번만 실행된다.
* 특징: static 필드 선언과 동시에 대입 연산자로 초기화하는 것보다 복잡한 초기화 로직(예: 리스트 객체 생성 후 다수의 기본 객체를 미리 add 해두는 작업 등)을 안전하게 처리할 수 있다.

### 7. 자바 Enum의 구조와 속성 활용

* Enum은 연관된 상수들의 집합을 정의하는 특수한 클래스 형태이다.
* 내부적으로 고유한 코드 수치나 문자열 설명(메시지)을 속성으로 포함하여 관리할 수 있다.
* 상수가 선언될 때 지정된 속성값들은 Enum의 private 생성자를 통해 인스턴스 필드에 매핑된다.

### 8. 보일러플레이트 코드의 개념과 대책

* 보일러플레이트(Boilerplate) 코드란 여러 곳에서 거의 수정 없이 반복적으로 재사용되는 지루하고 고정된 코드 덩어리를 의미한다.
* 클래스의 필드를 선언할 때마다 매번 수동으로 작성해야 하는 Getter, Setter, 생성자, toString 메서드 등이 대표적이다.
* 최근에는 롬복(Lombok) 라이브러리의 @Getter, @Setter 어노테이션 기법 등을 도입하여 이를 제거하고 보완한다.

### 9. String 형변환 방식의 차이점

* (String) value 방식 (명시적 타입 캐스팅):
  * 대상 객체가 실제로 String 타입이 아닐 경우 런타임에 ClassCastException을 발생시킨다.
  * value가 null인 상태에서 사용을 시도하면 NullPointerException이 발생하여 어플리케이션이 비정상 종료될 위험이 크다.
* String.valueOf(value) 방식:
  * 내부적으로 파라미터가 null인지 판단하여, 만약 null이라면 에러를 내지 않고 안전하게 문자열 "null"을 반환하도록 설계되어 있다. 객체가 안전하게 보호된다.

### 10. 스프링 계층형 아키텍처 어노테이션

* @Controller: 스프링 MVC 패턴의 Presentation 레이어로서 클라이언트의 HTTP 요청을 진입점으로 전달받아 View 이름이나 응답을 매핑하여 resolve 처리한다.
* @RestController: @Controller와 @ResponseBody가 결합된 형태이다. View를 반환하지 않고, 데이터 자체(JSON 또는 XML 등)를 HTTP 응답 바디에 직접 작성하여 클라이언트에게 전송한다.
* @Service: 비즈니스 로직이 수행되는 서비스 레이어를 명시한다. Spring AOP 측에서 이 어노테이션을 기반으로 트랜잭션 처리나 예외 처리 등의 부가 기능을 쉽게 묶어서 적용할 수 있게 해준다.

### 11. @Component와 @Configuration의 싱글톤 동작 차이

* @Component: 클래스 단위를 스프링 컨테이너의 Bean 객체로 등록하기 위한 기본 어노테이션이다. 컴포넌트 스캔을 통해 탐색되어 빈으로 등록된다. 내부에서 @Bean 메서드를 통해 다른 빈을 생성할 경우, 해당 메서드를 호출할 때마다 새로운 객체가 생성되어 싱글톤이 유지되지 않는다.
* @Configuration: 설정 파일 역할을 수행하는 클래스에 명시한다. 내부에 작성된 @Bean 메서드들이 반환하는 객체를 스프링 컨테이너가 싱글톤으로 철저하게 관리하도록 보장한다. CGLIB 프록시 기술을 사용하여 클래스를 감싸고, 메서드가 호출될 때 컨테이너에 이미 빈이 등록되어 있다면 기존 빈을 반환하고 없으면 새로 생성하여 등록하는 원리로 동작한다.

### 12. CORS와 CSRF 개념 정리

* CORS (Cross-Origin Resource Sharing):
  * 웹 브라우저의 Same-Origin Policy(SOP, 동일 출처 정책)에 의해 기본적으로 임의의 사이트가 다른 사이트의 API 리소스를 함부로 호출할 수 없도록 통제하는 보안 메커니즘이다. 허용된 오리진만 자원에 접근할 수 있게 조율한다.
* CSRF (Cross-Site Request Forgery):
  * 사용자가 이미 특정 사이트(예: bank.com)에 로그인하여 브라우저 쿠키(세션 ID)를 보유한 상태에서, 악성 공격용 사이트에 접속했을 때 발생한다. 공격자가 심어둔 악성 스크립트를 통해 브라우저가 bank.com으로 자동으로 가짜 요청을 보내게 만들며, 서버는 전송된 쿠키를 보고 정상 요청으로 오인하여 돈을 송금하는 등의 민감한 명령을 수행하게 만드는 공격 기법이다.

### 13. JSON 처리 라이브러리 특징 비교

* Jackson: 실무와 스프링 프레임워크 생태계에서 가장 널리 사용하는 범용 JSON 매퍼 라이브러리이다. 스트리밍 기반의 API를 제공하여 대용량 JSON 처리에 우수한 성능을 보이며, 다양한 어노테이션 설정을 지원한다. ObjectMapper 클래스를 핵심으로 사용한다.
* Gson: 구글에서 개발한 JSON 라이브러리로, 매우 가볍고 직렬화(Object to JSON) 및 역직렬화(JSON to Object) 동작 메커니즘이 직관적이고 단순하다. 다만 Jackson에 비해 복잡한 커스텀 기능이나 대규모 스트리밍 처리 최적화는 제한적이다.
* org.json: 가장 초창기 형태의 원시적인 JSON 라이브러리이다. JsonObject, JsonArray 객체를 직접 생성하고 다루어야 하며, 자바 객체와 자동으로 매핑해 주는 데이터 바인딩 API(리플렉션 기반 매핑)를 자체 제공하지 않으므로 개발자가 일일이 수동으로 제어 코드를 구현해야 한다.

### 14. 정적 내부 클래스(Static Nested Class)의 메모리 격리

* 일반 내부 클래스는 외부 클래스의 인스턴스 참조를 숨은 필드로 가지기 때문에 외부 객체가 메모리에서 해제되는 것을 방지하여 메모리 누수를 유발할 수 있다.
* 반면 정적 내부 클래스(Static Nested Class)는 외부 클래스의 인스턴스(this)에 대한 참조 종속성이 완전히 제거된다. 외부 객체 생성 없이 독립적으로 인스턴스를 생성할 수 있다.
* JVM 컴파일 시 Outer$Inner.class 형태로 분리되어 Method Area(MetaSpace) 영역에 적재되며, 필요할 때마다 Heap 영역을 독립적으로 할당받아 인스턴스를 유지하므로 무의미한 외부 인스턴스 동시 유지를 방지하여 메모리 효율성에 최적화되어 있다.

### 15. ThreadLocal의 독립적 공간 격리 및 주의점

* 프로세스 내부에서 실행되는 각 스레드(Thread)별로 완전히 독립된 격리 변수를 할당하고 관리할 수 있도록 지원하는 클래스이다.
* 일반적인 멀티 스레드 환경에서는 Heap 영역에 생성된 공유 객체의 데이터를 동시에 수정하다가 race condition 동시성 문제가 발생할 수 있다. ThreadLocal을 활용하면 각 스레드가 고유한 저장소를 가지므로 자원 공유 없이 격리된 값을 안전하게 유지할 수 있다.
* 치명적인 주의점: 실무에서는 톰캣과 같은 WAS 환경에서 스레드 풀(Thread Pool)을 사용한다. 작업이 종료된 스레드는 소멸하지 않고 풀로 반환되는데, 이때 ThreadLocal 내부의 데이터를 명시적으로 remove하여 초기화하지 않으면 해당 스레드를 재사용하는 다음 사용자가 이전 사용자의 민감한 데이터를 그대로 노출받거나 참조하게 되는 심각한 보안 오동작 버그가 발생한다.

### 16. 스레드 풀 포화 정책 (Saturation Policies)

* Thread Pool이 완벽하게 포화 상태(더 이상 작업을 수용할 수 없는 임계점)일 경우 새로 유입되는 작업에 대해 적용하는 거절 대응 정책이다.
* ThreadPoolExecutor 인프라를 설정할 때 내부 제어 로직은 세 가지 핵심 수치를 기준으로 스레드 확장을 조율한다.
  * corePoolSize: 상시 대기 상태를 유지하는 기본 스레드 개수.
  * workQueueSize: 작업이 가득 찼을 때 대기하는 임시 큐의 크기.
  * maxPoolSize: 큐까지 가득 찬 경우 최대로 확장하여 생성할 수 있는 스레드의 최대 한도 개수.
* 만약 maxPoolSize 한도 수치까지 스레드가 가득 차고 workQueueSize 대기열까지 완전히 포화된 상태에서 새로운 작업 요청이 추가 인입되면, 시스템은 RejectedExecutionHandler 인터페이스 구현체 동작을 통해 거절 액션을 수행하게 된다. 이 거절 연산 메커니즘을 Thread Pool 포화 정책이라고 명칭한다.

### 17. @Repository 어노테이션의 존재 이유와 예외 추상화

* Interface 기반 구현체 혹은 Redis, RDB 등 외부 데이터 체계와 직접 상호작용하는 영속성 계층 클래스 상단에 명시한다.
* 단순히 컴포넌트 스캔 대상 빈으로 등록하는 기능적 역할에만 머무르지 않는다.
* 핵심 역할: 데이터베이스나 인프라 드라이버별로 제각각 다르게 발생하는 하부 Checked Exception이나 벤더 종속적 예외 경로들을 스프링이 자체적으로 이해하고 공통 분모로 처리할 수 있도록, 스프링 전용 데이터 액세스 예외(DataAccessException) 체계로 자동 변환 및 추상화해 주는 결정적인 역할을 수행한다. 덕분에 런타임에 프록시 구현체를 동적으로 생성하는 Spring Data JPA 환경 등에서 하부 인프라 환경이 교체되더라도 비즈니스 서비스 로직이 전면 수정되는 파멸적 영향도를 최소화해 준다.

### 18. 빌드 포맷의 실행 메커니즘 차이 (JAR vs WAR)

* JAR (Java Archive):
  * 자바 프로젝트 전체 코드를 컴파일하여 압축한 범용 패키징 포맷이다. 일반적인 jar 파일은 단순 도구 모음(라이브러리) 형태를 취하므로 main 메서드가 없어 독립 기동이 불가능하다.
  * 스프링 부트 환경에서 빌드한 실행 가능한 Executable JAR(Fat JAR 형태)는 내부에 톰캣(Embedded Tomcat) 엔진을 내장 형태로 품고 있다. MANIFEST 파일에 메인 클래스가 지정되어 있어 외부 WAS 장치 없이 java -jar 명령어로 단독 구동이 가능하다. 단독 가상화 컨테이너 배포 구조에 최적화되어 있다.
* WAR (Web Application Archive):
  * 웹 애플리케이션 표준 구조에 맞추어 콘텐츠를 패키징한 전용 포맷이다.
  * 내부에 Embedded Tomcat을 포함하지 않고 배포되므로 단독으로 자체 구동할 수 없다. 반드시 외부 인프라에 별도로 설치된 정식 WAS 장치(Tomcat, JBoss, Jeus, WildFly 등 Servlet 컨테이너 실행 구조 체계)에 배포 플러그인 형태로 업로드해야만 작동한다. 이러한 중앙 집중식 WAS 배포 환경은 특정 서비스의 장애가 WAS 전체의 가용성 고갈로 이어져 시스템 장애 전파 위험이 존재한다는 특징을 가진다.

### 19. 시스템 콜(System Call)과 외부 전송 과정

* 유저 영역에서 작동하는 일반 애플리케이션이 운영체제 핵심 권한인 커널 모드(Kernel Mode) 자원을 안전하게 빌려 쓰기 위해 커널에 요청하는 유일한 권한 진입점 인터페이스이다.
* 애플리케이션은 시스템 보안 및 하드웨어 격리 정책으로 인해 네트워크 장비나 파일 I/O 물리 장치에 직접 접근할 권한이 전혀 없다. 파일 읽기/쓰기, 네트워크 패킷 송수신 연산 등은 무조건 커널을 거쳐야 한다.
* 특히 자바 애플리케이션이 원격지에 존재하는 데이터베이스(DB)로 쿼리 요청 데이터를 전송하고자 시도할 때, 해당 데이터 패킷을 물리 네트워크망 인터페이스 밖으로 내보내는 과정에서 반드시 이 시스템 콜을 호출하여 커널의 네트워크 프로토콜 스택 대행 동작을 거치게 된다.
