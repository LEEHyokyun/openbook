---
description: Spring framework의 core - JVM / Web MVC / Bean에 대하여
---

# 2-5. Spring Core #2  : JVM / Web MVC / Bean

### 1. 컴파일 개념과 ClassLoader 및 Spring Bean의 목적 구조적 차이점

* 자바 컴파일 및 로드 과정: 개발자가 작성한 `.java` 소스 파일은 자바 컴파일러(`javac`)에 의해 JVM이 해석하고 실행할 수 있는 바이너리 형태의 `.class` 바이트 코드로 변환된다. 이 변환된 `.class` 파일을 JVM 실행 중에 메모리로 읽어 들여 적재하는 주체가 바로 클래스로더(ClassLoader)다.
* ClassLoader의 역할: 클래스로더는 단순히 객체를 인스턴스화하는 것이 아니라, `new` 키워드로 객체를 생성할 수 있도록 클래스의 메타정보를 생성하고 JVM의 메타스페이스(Metaspace) 영역에 등록하여 사용 가능하도록 준비하는 핵심 과정을 수행한다. 클래스로더가 파일 적재를 완료하면 타입을 명세하는 `Class<?>` 메타 객체가 비로소 생성된다.
* Spring Bean의 생성 및 관리: 스프링 빈은 클래스로더를 통해 이미 타입 로드가 완료된 메타 정보를 바탕으로 구동된다. 스프링의 `ApplicationContext`가 시작되고 초기화되는 시점에 `BeanDefinition` 설정과 빈 리플렉션(Bean Reflection) 과정을 거쳐 실제 메모리에 살아 움직이는 객체(Bean Instance)를 동적으로 생성하고 생명 주기를 관리한다.
* 클래스로더와 스프링 빈의 명확한 비교 구분:
  * ClassLoader: 목적은 클래스를 JVM에 적재하여 사용 가능한 상태로 만드는 것이며, 산출물은 클래스의 메타정보인 `Class<?>` 객체다. 구동 시점은 JVM 실행 중 처음 해당 클래스가 참조되는 순간이다.
  * Spring IoC Container: 목적은 적재된 클래스를 가지고 실제 객체(Bean)를 생성하고 DI 및 생명 주기를 관리하는 것이며, 산출물은 실제 살아있는 싱글톤 등의 객체(Bean Instance)다. 구동 시점은 `ApplicationContext`가 시작 및 초기화되는 시점이다.

### 2. Host OS, JVM, WAS(Tomcat) 및 웹 애플리케이션의 물리적 실행 주체와 계층 구조

* JVM의 실행 주체: JVM의 실질적인 실행 주체는 호스트 운영체제(Host OS)다. JVM 자체가 OS 위에서 하나의 독립적인 프로세스로 실행되는 가상의 실행 기계이기 때문이다.
* 프로세스 내부의 서블릿 컨테이너 구동: 호스트 OS 위에서 구동되는 자바 프로세스(JVM) 내부에서 웹 애플리케이션 서버인 WAS(Tomcat, Jetty, WebLogic 등)가 실행된다. 이 WAS 인프라 내부에서 실질적인 웹 애플리케이션 스레드와 서블릿 컨테이너 환경이 완벽하게 구축된다.
* 인프라 아키텍처 계층 구조: 전체적인 실행 아키텍처는 \[운영체제(OS)] -> \[java 프로세스(JVM)] -> \[WAS (Tomcat 등)] -> \[웹 애플리케이션]의 수직적 계층 구조를 형성하며 유기적으로 맞물려 구동된다.

### 3. Web MVC Servlet Container 내부 스레드 생성과 스레드 풀(Thread Pool) 제어 구조

* 톰캣(Tomcat)의 HTTP 요청 접수 구조: 외부 클라이언트로부터 HTTP 요청 패킷이 인입되면, WAS의 엔트리 포인트인 톰캣 내부의 커넥터(Connector)가 이를 최초로 수신한다.
* 스레드 풀 기반의 동적 할당 프로세스: 커넥터가 요청을 접수하면 WAS 내부의 스레드 풀(Thread Pool) 구조인 `Executor`에게 작업을 위임한다. 스레드 풀은 미리 생성하여 유휴 상태로 보유하고 있던 스레드 중 하나를 꺼내어 해당 요청 작업을 처리하도록 동적으로 배정한다.
* 요청 처리 전담 스레드의 구동: 할당된 전담 스레드는 서블릿 컨테이너의 내부 실행 흐름을 타고 `HttpServletRequest` 및 `HttpServletResponse` 객체를 생성하여 최종 웹 애플리케이션 레이어로 진입하는 실행 엔진 역할을 수행한다.

### 4. DispatcherServlet의 웹 요청 처리 컴포넌트 간 상세 핵심 흐름 (Lifecycle)

* HTTP 요청이 서블릿 컨테이너로 진입하면, 스프링 웹 기술의 중심점인 `DispatcherServlet`이 프론트 컨트롤러(Front Controller)로서 모든 요청을 전면에서 총괄 접수한다.
* HandlerMapping을 통한 컨트롤러 탐색: `DispatcherServlet`은 인입된 URL 및 HTTP 메서드 정보를 바탕으로 `HandlerMapping` 컴포넌트를 호출한다. `HandlerMapping`은 매핑 정보를 전수 조사하여 해당 요청을 처리할 수 있는 적절한 타깃 컨트롤러(Handler)의 위치와 함께, 중간에 실행될 인터셉터 목록을 포함한 `HandlerExecutionChain`을 찾아 반환한다.
* HandlerAdapter를 통한 동적 실행 매커니즘: 컨트롤러의 형태가 다양할 수 있으므로 `DispatcherServlet`은 인터페이스 기반의 완충재인 `HandlerAdapter`를 호출한다. `HandlerAdapter`는 찾아낸 컨트롤러의 메서드 시그니처와 파라미터를 분석하여 리플렉션 기반으로 비즈니스 컨트롤러 객체를 동적으로 실행(Invoke)한다.
* 비즈니스 로직 수행 및 데이터 반환: 실행된 컨트롤러는 하부의 서비스 및 데이터 액세스 레이어를 거쳐 실질적인 비즈니스 로직을 수행한 뒤, 처리 결과 데이터와 뷰 정보를 담은 객체 또는 REST 형태의 원본 페이로드를 `HandlerAdapter`를 거쳐 `DispatcherServlet`에 최종 전달한다.

### 5. Servlet Container Filter와 Spring Container Interceptor의 경계 분리 및 구동 시점

* 필터(Filter)의 소속 및 동작 특성: 필터는 자바 표준 웹 기술인 서블릿 컨테이너(Servlet Container)의 핵심 구성요소다. 스프링 컨텍스트와 완전히 무관한 서블릿 컨테이너 레이어에 소속되어 있기 때문에, 요청이 스프링 핵심 엔진인 `DispatcherServlet`에 도달하기 전과 모든 처리가 끝난 후 응답이 나가는 최외곽 시점에 동작한다. 주로 웹 요청/응답 객체 자체를 가공하거나 글로벌한 인코딩, 보안 검증에 활용된다.
* 인터셉터(Interceptor)의 소속 및 동작 특성: 인터셉터는 스프링 컨테이너(Spring Container)가 직접 관리하는 구성요소다. 서블릿 컨테이너를 통과하여 `DispatcherServlet`이 요청을 접수한 이후, 실질적인 비즈니스 컨트롤러(Handler)가 실행되기 직전(`preHandle`)과 실행이 끝난 직후(`postHandle`), 그리고 뷰 렌더링이나 응답 직렬화가 완전히 완료된 시점(`afterCompletion`)으로 세분화되어 동작한다. 스프링 빈 컨텍스트 내부에 존재하므로 스프링 AOP, 공통 트랜잭션 상태 제어 및 사용자 권한 상세 체크 등에 정밀하게 접근할 수 있다.
* 전체적인 물리적 요청/응답 실행 라이프사이클 체인 흐름: Client -> Filter -> DispatcherServlet -> Interceptor (preHandle) -> Controller -> Interceptor (postHandle) -> Interceptor (afterCompletion) -> Filter -> Client

### 6. REST API 관점에서의 @ResponseBody와 ResponseEntity 처리 메커니즘 차이점

* @ResponseBody 직렬화 파이프라인: 컨트롤러 메서드 또는 클래스 상단에 `@ResponseBody`가 선언되어 있으면, 스프링 웹 엔진은 뷰 이름(View Name)을 찾는 정적 렌더링 과정을 전면 생략한다. 대신 리턴된 자바 객체 형태의 응답값을 JSON 등의 문자열 형태로 변환하기 위해 `HandlerAdapter` 하부의 `ReturnValueHandler` 메커니즘을 가동한다.&#x20;
* 이 과정에서 구체적으로 `HttpMessageConverter` 인터페이스의 구현체인 `MappingJackson2HttpMessageConverter` 등이 동작하여 객체를 JSON 스트림 데이터로 단순 직렬화(Serialization)하여 HTTP 응답 바디에 직접 기록한다.
* ResponseEntity의 가공 및 추상화 메커니즘: 컨트롤러가 단순 객체가 아닌 `ResponseEntity` 객체를 반환하는 경우, `HandlerAdapter`는 전용 응답 처리기인 `ResponseEntityReturnValueHandler`를 매핑하여 호출한다.&#x20;
* `ResponseEntity`는 단순 바디 데이터뿐만 아니라 HTTP 상태 코드(Status Code), HTTP 응답 헤더(Headers) 등 HTTP 메시지 전체 구성 요소를 자바 객체 수준으로 완벽하게 Wrapping한 상위 추상화 객체다.
* 이를 통해 개발자는 유연하게 상태 코드를 세분화하여 동적으로 변경할 수 있으며, 공통 Exception Handling 구조와 결합하여 정밀하게 가공된 표준화된 HTTP 전체 응답 객체 시스템을 완성할 수 있다.
